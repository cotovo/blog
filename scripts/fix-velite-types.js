const fs = require('node:fs')
const path = require('node:path')

const outputPath = path.join(process.cwd(), '.velite', 'index.d.ts')

const content = `// This file is generated by Velite and normalized for the homepage typecheck.

export interface VeliteTocItem {
  title: string
  url: string
  items: VeliteTocItem[]
}

export interface Post {
  title: string
  slug: string
  date?: string
  category?: string
  description?: string
  content: string
  toc: VeliteTocItem[]
  permalink: string
}

export declare const posts: Post[]
`

fs.writeFileSync(outputPath, content)
console.log('[velite-types] Normalized .velite/index.d.ts')
